/**
 * @deprecated Import from `escrow-seed` or use `useEscrowSimulation` for live state.
 * Kept so older imports keep working during refactors.
 */
export {
  defaultEscrowDeals,
  defaultEscrowSimulationState,
  ESCROW_STORAGE_KEY,
} from "./escrow-seed";
